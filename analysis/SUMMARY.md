# 진행 요약 (2026-07-06 ~ 07-07, 박현준)

> 한 줄 요약: **점수 산식은 그대로 두고**, 데이터 분석으로 위험 유형 6개를 도출했다.
> 규칙(envType)이 뭉뚱그리던 "고지·오지형" 132건을 표고·응급실 거리로 갈라냈고,
> 그중 400m+ 93곳이 실제 보정으로 반영됐다(산불철 기준 −5점, 34곳 등급 변화).

> ⚠️ **2026-08-02 보강**: 아래 클러스터링(4)과 분류기(6)는 입력 피처가 점수 엔진의
> 출력이라 **가중치의 근거로 인용할 수 없다**(순환). 무엇이 근거이고 무엇이 아닌지는
> [README.md](README.md) 상단 ⚠️ 절과 [25_safety_evidence_map.md](25_safety_evidence_map.md)에
> 정리했다. 순환 없는 실증은 아래 5번(119 데이터) 하나다.

---

## 1. 공모전 규정 확인 (동현 필독 ⚠️)

공식 노션 공지·FAQ 전문 확인 결과:

- **TourAPI는 실시간 호출 방식 강력 권고. 1차 심사 때 운영계정 인증키로 실제 호출
  내역을 검증함.** 로컬 저장(JSON 내장) 방식은 개발 기간 내 호출 이력이 없으면 심사
  불이익. 부득이하면 tourapi@knto.or.kr 로 별도 신청서.
  → 현재 하리노트는 seed 때만 호출하는 구조라 **서비스 경로에 실시간 TourAPI 호출
  추가가 필요** (예: 상세 페이지 detailCommon2 온디맨드 조회, 내장 JSON은 폴백 유지)
- 일정: 1차 서류 마감 **9/21(월) 16:00** → 10월 기능심사 → 10/21 발표 → 10/28 최종(상위 5팀)
- 1차 배점: 기획 30 / 완성도 30 / 데이터 활용 20 / 발전성 20 + **지역특화 가점 2 (강원 해당)**
- 웹은 URL 제출만 (스토어 등록 불필요), AI 도구 사용 무제한, "한국관광공사"·"KTO" 명칭·로고 사용 금지

## 2. 분석 파이프라인 (analysis/ 폴더, 01→10 순서 재현 가능)

1. **EDA** — 관광지 775건 중 envType 휴리스틱이 57%(443건)를 "일반 야외"로 뭉뚱그림.
   그 안에 표고 400m+ 99건 (산악 분류 55건보다 많음) → 분류 모델의 정량 근거
2. **데이터 수집** — 표고 2,086곳(SRTM 30m), 공식 서비스분류코드 v4.2(공모전 배포
   Drive, cat3 100% 매칭), 강원 119 구조출동 6,176건(소방안전 빅데이터 플랫폼)
3. **점수 엔진 Python 포팅** — 원본 vitest 경계 속성 20/20 재현 (`safety_engine.py`)
4. **클러스터링** — 1차 실패(극한 시나리오가 감점 상한 clamp에서 가중 신호를 지움)
   → 2차: 특보 직전 중간 강도 + 강수/강풍 분리 + cat2 블록 가중 축소.
   **k=6 채택 (bootstrap ARI 0.921 — 군집 *안정성*이지 타당성이 아니다)**
   ⚠️ 입력 피처가 엔진 감점(`s_*`)이라 규칙과 수렴한 4개 유형은 필연이다. 의미는
   규칙이 뭉뚱그린 구간을 **표고·응급실 거리로 갈라낸 것**에 있다
5. **외부 검증(119 데이터)** — 자연장소 사고는 구조가 유의하게 느림(시군 내 쌍대
   +3분, p=0.0008), 수난사고 분포 ↔ 수변·해안 관광지 분포 rho=0.49 p=0.038(탐색적).
   고지·오지형 ↔ 시군 구조시간은 사고 좌표 부재로 판정 불가
6. **분류기(RandomForest)** — 원천 피처만으로 CV acc **0.895** / macro-F1 0.870
   (envType 규칙 베이스라인 0.830 / 0.804). 규칙 오류 132건 중 116건 회수.
   중요도 1위 = 표고 → 성능 향상은 규칙이 안 쓰는 데이터에서 나옴
   ⚠️ **타깃이 자기 군집 라벨이다.** 0.895는 예측 성능이 아니라 "엔진 출력으로 만든
   군집을 원천 피처로 89.5% 복원 가능"이라는 **압축률**이다. 발표에서 "머신러닝이
   위험을 89.5% 정확도로 예측한다"고 말하면 안 된다
7. **산식 적용 영향 분석** — envType 보정(고지오지 & 400m+ → mountain, 93건):
   산불철 시나리오에서 93곳 −5점, **34곳 low→moderate 등급 변화**. 비대상 682곳 무변동

## 3. 위험 유형 6개

| 유형 | n | 비고 |
|---|---|---|
| 근교전천후 | 312 | 저지대·의료 양호 |
| **고지·오지형** | **132** | **신규 발견 — 표고 536m·응급실 19.5km, 현행 규칙은 "일반"** |
| 산악 | 123 | 규칙과 일치 |
| 해안 | 100 | 일치 |
| 수변 | 91 | 일치 |
| 실내 | 17 | 일치 |

## 4. 산출물

- `data/risk_types.json` — contentId → 유형 (775건). **무변경으로 배지 표시에 바로 사용 가능**
- `data/envtype_fix_impact.csv` — envType 보정 시 점수·등급 변화 명세 (93건)
- `risk_type_classifier.joblib`(재생성: 09 실행) — 신규 관광지·타지역 유형 할당
- 스크립트 01~10 + `safety_engine.py` + README.md (방법론 상세)
- 원자료 중 미포함: 119 사고 원본(1.8MB, bigdata-119.kr 무료 로그인 후 "강원특별자치도_관광지
  안전 사고 데이터"), 중간 parquet·joblib (스크립트로 재생성)

## 5. 다음 할 일

- [ ] 동현: TourAPI 실시간 호출 경로 협의 (규정 이슈 §1)
- [ ] 동현·현준: 유형 배지 UI + envType 보정 반영 여부 결정 (README '적용 방안' 참고)
- [ ] 현준: 주간보고서 2주차(7/11)에 분석 반영, 발표자료 "AI/통계 모델" 섹션 갱신
- [ ] 산림청 산불위험 API 활용신청 승인 확인 (승인 시 자동 전환)
